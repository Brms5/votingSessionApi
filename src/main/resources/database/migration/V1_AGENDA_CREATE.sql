CREATE TABLE Agenda (
                         id UUID NOT NULL,
                         topic varchar(255) NOT NULL,
                         title integer NOT NULL,
);

insert into Agenda(id, topic, title) values ();